def plug():
    x = -100
    # x is -100
    while x < 100:
        # while x is smaller than 100
        if 2 * x + 5 == 13:
            # if 2 multiplied by x + 5 is 13
            print(f"x = {x}")
            # x = whatever number os correct, in this case 4
        x += 1
        # this tests every number until finding the correct one

plug()